import PublicLayout from "@/layouts/PublicLayout.astro";
import { ResetPasswordForm } from "@/components/auth/ResetPasswordForm";

// The token is expected to be in the URL hash after #, e.g., /reset-password#access_token=TOKEN&...
// Astro cannot directly access URL hash server-side. This needs to be handled client-side in the React component.
// The component will listen for onAuthStateChange or parse it from window.location.hash.
// If Supabase allows configuring the redirect to put the token in query params, that would be easier.
// For now, we pass no token from Astro, assuming client-side handling in ResetPasswordForm.

<PublicLayout title="Reset Password">
  <ResetPasswordForm client:load />
</PublicLayout> 