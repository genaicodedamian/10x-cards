import PublicLayout from '@/layouts/PublicLayout.astro'; import ResetPasswordForm from
'@/components/auth/ResetPasswordForm.tsx'; // If error or other parameters are needed from URL for the form, they can be
accessed here // const errorFromUrl = Astro.url.searchParams.get('error'); // const messageFromUrl =
Astro.url.searchParams.get('message');

<PublicLayout title="Reset Password | 10xCards">
  <ResetPasswordForm client:load />
  {/* If you need to pass URL params to the React component, consider client-side script or a different approach */}
</PublicLayout>
